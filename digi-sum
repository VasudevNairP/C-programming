#include <stdio.h>

int main() {
  int n, temp, sum;
  printf("enter the number");
  scanf("%d", &n);
  while(n>0)
  {
    temp=n%10;
    sum+=temp;
    n/=10;
  }
  printf("sum of digits is %d",sum);
  return 0;
}
